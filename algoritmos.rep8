package main
import "fmt"
func main() {
	var a int
	fmt.Println("Escreva um número inteiro positivo")
	fmt.Scan(&a)
	fmt.Println()
	for i := 1; i <= a; i++ {
		if (a%i != 0) {
			continue
		} else {
			fmt.Println(a / i)
		}
	}
}
